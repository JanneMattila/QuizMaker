@model QuizViewModel

@{
    ViewData["Title"] = "Delete Quiz Responses";
    ViewData["EnableManagementLink"] = true;
}

<div>
    <h4>Delete Quiz responses</h4>
    <hr />

    <div>
        Your about to delete all the responses of this quiz:
    </div>

    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ID)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ID)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>

    <form asp-action="DeleteQuizResponses">
        <input type="submit" value="Delete" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete quiz responses?');" />
    </form>

    <hr />

    <div>
        <a asp-action="Index">Back to List</a>
    </div>
</div>
