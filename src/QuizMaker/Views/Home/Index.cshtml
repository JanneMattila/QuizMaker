@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div id="quizForm" class="form-group">
            Waiting for available quiz...
        </div>

        <div id="quizSubmit" class="form-group text-left" style="display:none">
            <input type="button" value="Submit" class="btn btn-primary quiz-submit" onclick="formSubmitCheck(); return false;" />
            <div id="quizSubmitError" class="text-danger quiz-submit-error"></div>
        </div>
    </form>
</div>

@section Scripts {
    <script src="~/lib/requirejs/require.js" data-main="/js/quiz.js"></script>
}